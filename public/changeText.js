//get the inner text first
let text = document.getElementById("gameDescription").innerText;

// put the text to innerHTML
let textHTML = document.getElementById("gameDescription");
textHTML.innerHTML = text;